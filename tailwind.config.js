/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",               // Include the `index.html` file at the root.
    "./src/**/*.{js,ts,jsx,tsx}", // Include all files in the `src` directory and its subdirectories 
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

